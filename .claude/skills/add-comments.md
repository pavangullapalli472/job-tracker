---
description: Add beginner-friendly comments to a function or block of code
---

The user will paste or reference a function or block of code from `job_tracker.py`.

Add clear, beginner-friendly inline comments to the code:
- Explain what each meaningful line or block does
- Keep comments short and to the point (one line max per comment)
- Don't state the obvious (e.g. don't write `# increment i` for `i += 1`)
- Focus on the *why* and *what*, not just restating the code
- Use plain English — no jargon

Return the full function with comments added. Do not change any of the actual code logic.
